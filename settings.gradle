pluginManagement {
    repositories {
        maven { url = 'https://repo.spring.io/snapshot' }
        gradlePluginPortal()
    }
}
rootProject.name = 'Ureka_plus'

include 'billing_api'
include 'billing_batch'
include 'billing_message'
